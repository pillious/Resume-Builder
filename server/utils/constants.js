/**
 * Global Constants
 * 
 * USAGE: const constants = require("../../utils/constants");
 */

const PORT = process.env.PORT || 8000;

const DEFAULT_LIMIT = 50;

module.exports = {
    DEFAULT_LIMIT,
    PORT
};
